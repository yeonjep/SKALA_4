document.querySelector('#signupForm').addEventListener('submit', async e => {e.preventDefault();const response=await fetch('/api/auth/signup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:username.value,password:password.value})});if(response.ok){location.href='/login';return;}const body=await response.json().catch(()=>({message:'회원가입 실패'}));message.textContent=body.message;});
